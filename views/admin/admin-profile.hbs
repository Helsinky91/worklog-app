<div>
    <h2>My Teamlead profile</h2>
        {{#with adminDetails}} 
         <img src="{{photo}}" alt="{{fistName}}'s pic" width="150px">
            <h4>Welcome back, {{firstName}}!</h4>
   

     <button class="btn"><a href="/profile/edit/{{_id}}">Edit your profile</a></button>
       {{/with}} 
    <hr>
 
  <div>
    <h2>Next events</h2>
{{!-- carrusel eventos --}}
      <p>Create, edit and check all events!</p>
      <button class="btn"><a href="/events">Take me to events</a></button>
      
    <hr>

      <div>  
        <h2>Active workers</h2>
        <button class="btn"> <a href="/admin/all-users">List of all workers</a> </button>
    
            {{#each allUsers}}
              {{#if isWorking}}
          <p>{{firstName}} {{lastName}}</p> 
          <p>Department: {{department}}</p> 

        <button class="btn"><a href="/profile/edit/{{_id}}">Edit this Worker's info</a></button>
        
        <form action="/admin/worklog-validation/{{_id}}" method="POST">
           <button class="btn">Validate Worklogs</button>
        </form>
     <hr>
            {{/if}}
    
          {{/each}}
    </div>

</div>